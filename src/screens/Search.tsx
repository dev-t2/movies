import React, { memo } from 'react';
import { Text, View } from 'react-native';

const Search = () => {
  return (
    <View>
      <Text>Search</Text>
    </View>
  );
};

export default memo(Search);
