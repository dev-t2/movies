import React, { memo } from 'react';
import { Text, View } from 'react-native';

const Detail = () => {
  return (
    <View>
      <Text>Detail</Text>
    </View>
  );
};

export default memo(Detail);
