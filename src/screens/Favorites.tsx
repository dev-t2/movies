import React, { memo } from 'react';
import { Text, View } from 'react-native';

const Favorites = () => {
  return (
    <View>
      <Text>Favorites</Text>
    </View>
  );
};

export default memo(Favorites);
